# cmtx-web-frontend
Frontend mínimo que muestra un botón **Consultar** y llama a la API pública.

## Qué incluye
- Dockerfile + app mínima en Python (sirve HTML simple)
- Deployment + Service (+ Route opcional) en Helm
- Vars de entorno para apuntar a la API
- CI/CD: build imagen + deploy Helm